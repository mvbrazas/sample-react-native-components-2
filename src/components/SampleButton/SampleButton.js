import { Text, TouchableOpacity } from "react-native";

const SampleButton = () => {
  return (
    <TouchableOpacity>
      <Text>SampleButton</Text>
    </TouchableOpacity>
  );
};

export default SampleButton;